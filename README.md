# C++ Name Sorting Program (Internship Task)

This repository contains my solution for the C++ internship test task.

## 📁 Files Included
- `sortNames.cpp` — Main C++ program  
- `README.txt` — Explanation of the program and how to run it  

## ✅ What the program does
The program:
- Reads a list of names from the user  
- Stores them in a `std::vector<string>`  
- Displays the original list  
- Allows the user to choose:
  - Ascending (A → Z)  
  - Descending (Z → A)  
- Prevents empty name input  
- Displays the sorted result  

## 🛠️ How to compile and run

```bash
g++ sortNames.cpp -o sortNames
./sortNames
